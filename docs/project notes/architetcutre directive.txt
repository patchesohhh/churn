it's very important to me to build this with minimal dependancies between files. I'm going to be vibe coding most of this, so I need the AI to be able to understand these files with as little context of other files as possible. I'm not saying I want ZERO dependency, but I would rather ask an LLM to look at a few files that interact together to find a bug rather than make it look through the context of the full codebase. It should be modular, with dependencies only when necessary.